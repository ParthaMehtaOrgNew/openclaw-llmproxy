apiVersion: v1
kind: Service
metadata:
  name: openclaw-proxy-service
  namespace: openclaw
spec:
  selector:
    app: openclaw-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
