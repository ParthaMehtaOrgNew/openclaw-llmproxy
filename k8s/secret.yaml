apiVersion: v1
kind: Secret
metadata:
  name: openclaw-proxy-secrets
  namespace: openclaw
type: Opaque
stringData:
  PROXY_API_KEY: "changeme"
  # Add per-backend keys here if using key management:
  # OPENAI_API_KEY: "sk-..."
  # ANTHROPIC_API_KEY: "sk-ant-..."
